/**
 * Internal build-time metadata records used by the Maven plugin.
 *
 * <p>These records are public only where cross-package plugin implementation access requires it;
 * they are not a supported application API.
 */
package com.ll.metrics.latency.maven.model;
