/**
 * Internal ASM bytecode scanning and instrumentation implementation.
 *
 * <p>These classes are not an application API. They are kept separate from the Mojo so ASM details
 * remain isolated inside the Maven plugin.
 */
package com.ll.metrics.latency.maven.asm;
